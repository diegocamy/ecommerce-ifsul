import { SpinnerElement } from "./Spinner.styles";

const Spinner = () => {
  return (
    <SpinnerElement>
      <i className="fas fa-spinner"></i>
    </SpinnerElement>
  );
};

export default Spinner;
